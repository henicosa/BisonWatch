'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var expand = require('./expand.js');
var leftJoin = require('./leftJoin.js');
var replaceNully = require('./replaceNully.js');

function complete(expandKeys, replaceNullySpec) {
  const _complete = (items) => {
    const expanded = expand.expand(expandKeys)(items);
    const joined = leftJoin.leftJoin(items)(expanded);
    return replaceNullySpec ? replaceNully.replaceNully(replaceNullySpec)(joined) : joined;
  };
  return _complete;
}

exports.complete = complete;
//# sourceMappingURL=complete.js.map
