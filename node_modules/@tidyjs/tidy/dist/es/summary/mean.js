import { mean as mean$1 } from '../helpers/summation.js';

function mean(key) {
  const keyFn = typeof key === "function" ? key : (d) => d[key];
  return (items) => mean$1(items, keyFn);
}

export { mean };
//# sourceMappingURL=mean.js.map
