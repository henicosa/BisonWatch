import { median as median$1 } from 'd3-array';

function median(key) {
  const keyFn = typeof key === "function" ? key : (d) => d[key];
  return (items) => median$1(items, keyFn);
}

export { median };
//# sourceMappingURL=median.js.map
