import { variance as variance$1 } from 'd3-array';

function variance(key) {
  const keyFn = typeof key === "function" ? key : (d) => d[key];
  return (items) => variance$1(items, keyFn);
}

export { variance };
//# sourceMappingURL=variance.js.map
